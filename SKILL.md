---
name: literature-deep-skill
description: 学术文献深度处理工具集——支持「逐字摘抄、归纳总结、叙述改写、要素抽取(研究背景/相关工作/数据集/方法/实验)、PPT 生成」六类原子能力。**专门为处理多篇中文学术文献综述与 PPT 写作设计**。当用户提供一篇或多篇 PDF/MD 格式文献,并要求"总结"、"摘抄"、"提取研究背景/相关工作/数据集/方法/实验"或"做成 PPT"时调用此 skill。与 `paper-summarizer` 的区别:本 skill 强调「多模式区分」(摘抄 vs 总结 vs 叙述,三者绝不混淆),并内置对中文学术 markdown 章节标题多种命名(`## 3.` / `## 三、` / `## 研究背景与动机` / `## 背景与动机`)的容错。
---

# Literature Deep Skill — 学术文献深度处理工具

## 适用范围

本 skill 用于处理用户的学术文献(单篇或批量),完成以下任一任务:

1. **总结归纳文献**(MODE-B):重组并撰写有自己叙述结构的总结。
2. **逐字摘抄文献章节**(MODE-A):原文复制,零改写,零归纳。
3. **完整段落叙述**(MODE-C):基于多篇文献写成可直接用作论文/汇报正文的连贯长文。
4. **要素抽取**:研究背景、相关工作、数据集、实验方法、实验结果、局限性、贡献点等结构化字段。
5. **学术 PPT 生成**:汇报型 PPT、组会 PPT、论文阅读 PPT(中文优先,学术克制风格)。

## 不适用范围

- 纯文献检索 / DOI 查找 → 用 `paper-lookup` / `bgpt-paper-search`
- 论文写作 / 投稿润色 → 用 `research-writing-skill` / `scientific-writing`
- 单篇英文论文的快速通读总结 → 用 `paper-summarizer:paper-summarizer`(更轻量)

---

## 核心原则:三种工作模式严格区分(本 skill 最重要的设计)

**这是过往多次失败案例总结出的第一原则**。当用户说"帮我处理这些文献"时,**必须在动手前用 AskUserQuestion 明确"模式"**——否则会反复返工(典型踩坑路径:先做归纳被退回 → 改成"PPT 页式重组"再被退回 → 改成"半摘抄"仍被退回 → 改为完全逐字摘抄才被接受)。

### MODE-A 逐字摘抄(Verbatim Extraction)

- **定义**:把原文 100% 原封不动地复制到输出文档,零改写、零总结、零删减、零重组。
- **触发关键词**:用户说"摘抄"、"直接复制"、"原文"、"不要总结"、"我自己用来写 PPT"。
- **必须**:用脚本化抽取(`scripts/extract_sections.sh`)而非"人手挑选要点",因为后者一定会偷偷归纳。
- **输出组织**:**按论文逐篇组织**,绝不按 PPT 页/主题/范式重组。每篇文献一节:`## #N — [论文标题]`,下设 `### 研究背景(原文逐字摘抄)`、`### 相关工作(原文逐字摘抄)` 等子节。
- **失败信号**:如果用户说"我要的是直接摘抄,你还在总结归纳"——立即停止,切换到脚本提取模式重做。

### MODE-B 归纳总结(Structured Summary)

- **定义**:重新组织原文,用自己的话写出结构化摘要(贡献点、方法、结果)。
- **触发关键词**:用户说"总结"、"归纳"、"摘要"、"帮我看看这篇讲了什么"。
- **输出模板**:参见 `references/summary-templates.md`(基本信息表 + TL;DR + 贡献 + 方法 + 实验 + 局限 + 个人点评)。
- **每篇文献一个独立 .md 文件**;批量处理时另出一份 `00-OVERVIEW.md` 总览索引。

### MODE-C 完整段落叙述(Narrative Document)

- **定义**:基于多篇文献的素材,写成**有完整段落、有起承转合、可直接当论文章节使用**的连贯长文。
- **触发关键词**:用户说"完整叙述"、"不要只给论点"、"写成文章"、"做成研究背景章节"。
- **典型失败**:只给 bullet point + 数据表。**用户的纠正:"我需要的是文字的完整叙述,不要只给论点"**。
- **输出特征**:每节由 2~5 个完整段落组成,数据嵌入文中并用 `[#N]` 标注来源,绝不出现裸 bullet。

---

## 工作流(严格按顺序执行)

### Step 1 — 摸清输入与意图(必做)

用 AskUserQuestion 一次问清以下 3~4 项:

1. **模式**:MODE-A 摘抄 / MODE-B 总结 / MODE-C 叙述 / 还是「全套」(先 B,再 A 出素材,最后 C 写章节)
2. **抽取要素**:研究背景 / 相关工作 / 数据集 / 实验方法 / 实验结果 / 局限性 / 全部
3. **是否需要 PPT**:是 → 走 `references/ppt-design.md`;否 → 仅输出 markdown
4. **输出语言**:默认中文(英文术语保留)。如用户原始指令已写明,跳过本问。

如用户已经在原始消息里说清楚以上 3 项,**跳过 AskUserQuestion,直接进入 Step 2**。

### Step 2 — 处理输入文献

#### 2.1 输入是 PDF

调用 `pdftotext` 或 `markitdown` 转 markdown。如需提取图表,用 `pdftoppm -r 150 -f N -l N -png` 渲染关键页。

#### 2.2 输入是已有 markdown 总结

直接读取。**注意 markdown 中的章节标题在不同文献中可能用以下任意一种命名**(见教训 2):

```
## 3. 研究背景与动机     # 阿拉伯数字 + 点
## 三、研究背景(Background)   # 中文数字 + 顿号
## 研究背景与动机         # 无编号
## 背景与动机             # 无"研究"前缀
```

提取脚本必须用**关键词 fuzzy match**(包含 "研究背景" OR "背景" OR "动机"),不能基于 `## 3` 这种数字编号——否则会漏掉 1/3 以上的文献。

### Step 3 — 执行模式对应的动作

| 模式 | 执行方式 |
|---|---|
| MODE-A | 调用 `scripts/extract_sections.sh`,把原文章节按文献逐篇拼接到一个 `.md` 文件 |
| MODE-B | 按 `references/summary-templates.md` 模板,逐篇生成独立 `.md` + 一份 `00-OVERVIEW.md` |
| MODE-C | 先调一次 MODE-A 拿到原文素材,再基于素材以完整段落写成长文 |

### Step 4 — 如需生成 PPT

读取 `references/ppt-design.md`,按其规定的视觉规范(色彩、字体、版式、章节结构)用 `scripts/build_pptx.py` 模板生成。**禁止裸 python-pptx 从零手写**(历史教训:输出粗糙、配色违和、易出现中文乱码)。

### Step 5 — 交付前自检

参见 `references/pitfalls.md` 的清单:

- [ ] 模式有没有被偷偷混淆(MODE-A 里夹了归纳话术?MODE-C 里只有 bullet?)
- [ ] 章节标题别名是否全部命中(是否有"研究背景"为空的文献?)
- [ ] 引用编号 `[#N]` 是否与源文档一致
- [ ] 数据是否保留原文细节(数字、年份、数据集名,不要写"等")
- [ ] 是否中文优先,英文术语保留
- [ ] PPT 是否无文本溢出、无中文乱码、无图像变形

---

## 关键资源索引

| 文件 | 用途 |
|---|---|
| `references/extraction-modes.md` | 三种模式的详细判别与执行细则 |
| `references/section-mapping.md` | 中文学术 markdown 章节标题别名映射表 |
| `references/summary-templates.md` | MODE-B 的标准摘要模板 |
| `references/ppt-design.md` | 学术 PPT 视觉规范与版式 |
| `references/pitfalls.md` | 历史踩坑清单与自检 checklist |
| `scripts/extract_sections.sh` | 关键词 fuzzy match 的章节抽取脚本 |
| `scripts/build_pptx.py` | python-pptx 学术风模板(深蓝主色 + 橙色强调) |

---

## 历史教训速查(必读)

1. **不要先动手再问需求**:多轮返工的根因往往是没在 Step 1 问清"MODE-A 还是 MODE-B 还是 MODE-C",而是默认按 MODE-B 处理。
2. **不要凭记忆 / 凭感觉抽原文**:人手抽取一定会偷偷归纳。用脚本。
3. **不要按 PPT 页重组多篇文献**:用户要的多半是「按文献逐篇」的素材包,不是「按主题/页面」的成品。
4. **不要省略数据**:"X 报告显示某国约 3000 多例事故" ❌ → "X 报告显示某国 3522 例事故"(保留原文精确数字) ✅
5. **不要默认输出英文**:中文是默认,即便源文献是英文。
6. **不要把"汇报"理解为只要 bullet**:用户要"叙述"时,产出必须是完整段落。
7. **PPT 不要手写 layout**:复用模板或 office-academic-skill 的脚本,否则一定丑。
